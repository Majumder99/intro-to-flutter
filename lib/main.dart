import "package:flutter/material.dart";

void main() {
  runApp(MaterialApp(
      title: "My flutter app",
      home: Scaffold(
          body: Center(
        child: Text(
          "Hello sourav",
          textDirection: TextDirection.ltr,
        ),
      ))));
}
